<Window x:Class="SnakeGame.MainWindow"
        x:Name="MainWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SnakeGame"
        mc:Ignorable="d"
        Title="MainWindow" Height="1200" Width="1600" KeyDown="MainWin_KeyDown">
    <Grid>
        <Canvas x:Name="GameCanvas" Focusable="True">
            <Canvas.Background>
                <ImageBrush ImageSource="pack://application:,,,/Images/background.jpg" />
            </Canvas.Background>
        </Canvas>

        <TextBlock x:Name="ScoreTextBlock" Text="Score: 0" Foreground="White" HorizontalAlignment="Left" Margin="10" VerticalAlignment="Top"/>
        <Button x:Name="RestartButton" Content="Restart" HorizontalAlignment="Center" VerticalAlignment="Bottom" Visibility="Collapsed" Click="RestartButton_Click"/>
        <Button x:Name="PauseButton" Content="Pause" Width="100" Height="50" HorizontalAlignment="Right" VerticalAlignment="Top" Click="PauseButton_Click" />
    </Grid>
</Window>
